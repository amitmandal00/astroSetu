# Use legacy peer deps to resolve dependency conflicts
# This is needed for Vercel builds with vite/vitest dependencies
legacy-peer-deps=true

# Don't fail on optional dependencies
optional=true

# Don't fail on peer dependency conflicts
strict-peer-deps=false

# Continue on audit failures
audit=false

# Don't show funding messages
fund=false
