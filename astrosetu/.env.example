NEXT_PUBLIC_APP_NAME=AstroSetu
NEXT_PUBLIC_TAGLINE=Bridging humans with cosmic guidance

# MVP-Critical Production Flags (DO NOT CHANGE IN PRODUCTION WITHOUT EXPLICIT APPROVAL)
# These flags control payment bypass, bundle availability, and test mode behavior
# Default values shown are SAFE for production

# Bundle Reports Feature Flag
# Set to "true" only if bundles are fully tested and meet MVP bundle-level payment guarantee
# RECOMMENDED: Keep "false" in production until bundle redesign is complete
NEXT_PUBLIC_BUNDLES_ENABLED=false

# Payment Bypass for Test Users (LOCAL/PREVIEW ONLY)
# NEVER set to "true" in production unless explicitly testing payment flows
# This bypasses Stripe for test users - only safe in local/preview environments
BYPASS_PAYMENT_FOR_TEST_USERS=false

# Production Test Session Bypass (EXPLICIT OPT-IN REQUIRED)
# Allows prodtest_ sessions to bypass Stripe in production
# WARNING: Only enable if you explicitly need production test sessions
# NEVER enable without understanding the security implications
ALLOW_PROD_TEST_BYPASS=false

# Demo Mode (LOCAL/PREVIEW ONLY)
# Enables demo/test mode for development
# NEVER set to "true" in production
AI_ASTROLOGY_DEMO_MODE=false

# Vercel Environment (informational - set automatically by Vercel)
# VERCEL_ENV=production
